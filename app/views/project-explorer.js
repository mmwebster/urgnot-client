import Ember from 'ember';

export default Ember.View.extend({
  classNames: ['project-explorer'],
  origin1a1EnabledL1: true,
  origin2EnabledL2: true,
  // actions: {
  //   focus: function(level, order) {
  //
  //   },
  // },
});
