import { moduleFor, test } from 'ember-qunit';

moduleFor('view:app/settings', 'Unit | View | app/settings');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
